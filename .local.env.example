# Koios Provider
export API_URL=https://api.koios.rest/api/v1
export API_TOKEN=your-koios-api-token

# Blockfrost Provider
export BLOCKFROST_URL=https://cardano-mainnet.blockfrost.io/api/v0
export BLOCKFROST_PROJECT_ID=your-blockfrost-project-id

# Network
export NETWORK_NAME=mainnet

# At least one provider (Koios or Blockfrost) must be configured.
# Run live tests with:
#   source .local.env && LIVE_TEST=true npx vitest run src/cardano/cardanoApi.live.test.ts
