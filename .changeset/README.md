# Changesets

This directory holds [changesets](https://github.com/changesets/changesets). Each
pull request that changes published behavior should add one with `npx changeset`,
describing the change in plain language for the changelog.
